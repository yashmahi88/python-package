name: Main Workflow

on:
  push:
    branches:
      - development
      - release
    tags:
      - '*'

jobs:
  call_publish_package:
    uses: yashmahi88/python-package/.github/workflows/main.yml@development
    with:
        LANGUAGE: ${{ vars.LANGUAGE}}
    #   project_key: ${{ vars.PROJECT_KEY }}
    #   project_name: ${{ vars.PROJECT_NAME }}
    # secrets: inherit
 